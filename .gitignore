# Build artifacts — all regenerated by `make`
*.o
*.d
*.elf
*.bin
boot/kernel_sectors.inc
*.dis

# OS disk image (padded build output + any old manual concatenation)
os-image
os-image.bin

# ATA data disk (regenerated by `make disk.img`; holds test data)
disk.img
